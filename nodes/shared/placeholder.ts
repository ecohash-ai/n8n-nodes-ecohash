// Temporary placeholder so the TypeScript project (see tsconfig.json's
// "include" globs) has at least one input file before real node and
// credential source is added in later tasks. Safe to delete once
// nodes/ or credentials/ contain actual .ts source files.
export {};
